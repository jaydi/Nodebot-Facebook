<script>
  function showMessage(id) {
    window.location.href = "/messages/" + id;
  }
</script>

<section class="first">

  <div class="container">

    <% if @celeb.pending? %>
        <div class="row">
          <div class="col-lg-12 text-center">
            <p>가입심사가 진행중입니다. 심사가 완료되면 메신저를 통해 안내해드립니다.</p>
          </div>
        </div>
    <% elsif @messages.blank? %>
        <div class="row">
          <div class="col-lg-12 text-center">
            <p>수신한 메시지가 없습니다.</p>
          </div>
        </div>
    <% else %>
        <% @messages.each do |m| %>
            <div class="row">
              <div class="col-lg-8 col-lg-offset-2 fa-border clickable" onclick="showMessage(<%= m.id %>)">
                <h3><%= m.sender.name %></h3>
                <p><%= m.text %></p>
              </div>
            </div>
        <% end %>
    <% end %>

  </div>

</section>